export default {
    PRODUCTS(state) {
        return state.products;
    },

    CAMERAS(state) {
        return state.cameras;
    },

    TV(state) {
        return state.tv;
    },

    PRODUCT(state) {
        return state.product;
    },

    CART(state) {
        return state.cart
    }
}